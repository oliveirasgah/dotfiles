#
# ~/.zprofile
#
# Sourced by zsh for login shells, before .zshrc.
# .zshrc is sourced automatically by zsh for interactive shells,
# so we don't source it from here.

# Added by LM Studio CLI (lms)
export PATH="$PATH:/home/gabriel/.lmstudio/bin"
# End of LM Studio CLI section
